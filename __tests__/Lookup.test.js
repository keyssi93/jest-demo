//Lookup.test.js
const lookup = require("../Lookup");

/* 
Write a Suite with the following specs
  1. Test 'likes' for the specified user
  2. Test 'age' to be less than 100 for the specified user
  3. Test error message for unknown user.
*/

// YOU CODE GOES HERE














/*
describe('Testing lookup func', () => {
    test("lookup(username, property) should return likes for the specified user.", () => {
      const actual = lookup('doom', 'likes');
      const expected = ['Javascript', 'Python', 'C'];
  
      expect(actual).toEqual(expected);
    });

    test("lookup(username, property) should return the age for the specified user", () => {
      const actual = lookup('kovrov', 'age');
  
      expect(actual).toBeLessThan(100);
    });

    test("lookup(username, property) with unknown user should throw an error with the correct message", () => {
      expect(() => {
        lookup('nobody', 'likes');
      }).toThrowError("Could not find user");
    });

  });
*/